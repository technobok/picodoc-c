[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "picodoc-c"
version = "0.1.0"
description = "Python ctypes bindings for picodoc-c"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]
